language: python
virtualenv:
  system_site_packages: true
python:
  - "2.7"
#  - "3.4"
#  - "nightly"
# command to install dependencies
install: "sudo apt-get install -qq libatlas3gf-base libatlas-dev python-scipy python-numpy && pip install -r requirements.txt"
# command to run tests
script: python setup.py test